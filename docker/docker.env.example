# -----------------------------
# Shared Docker configuration
# -----------------------------
REQUIRE_COURSE_CONTENT_FOR_PUBLISH=true

# -----------------------------
# Node backend secrets/config
# -----------------------------
JWT_SECRET=change_me_jwt_secret
REFRESH_SECRET=change_me_refresh_secret
JWT_ISS=gateway.api
JWT_AUD=web.app
PWD_PEPPER=change_me_pwd_pepper
SESSION_SECRET=change_me_session_secret

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3001/api/v1/auth/google/callback

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

FRONTEND_URL=http://localhost:3000
CORS_ALLOWLIST=http://localhost:3000

# -----------------------------
# Python backend secrets/config
# -----------------------------
OPENAI_API_KEY=
DEEPGRAM_API_KEY=
PYTHON_ALLOWED_ORIGINS=http://localhost,http://localhost:3000,http://localhost:3001
